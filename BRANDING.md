# Startup Xpand brand asset

The production launcher icon uses the approved blue geometric logo supplied on 2026-07-24.

- Android icon resource: `app/src/main/res/drawable-nodpi/ic_launcher_brand.jpg`
- Manifest icon and round icon both reference the approved resource.
- Release signing material remains stored in the protected repository secret `ANDROID_SIGNING_BUNDLE_BASE64`; raw secrets are never committed to Git history.
